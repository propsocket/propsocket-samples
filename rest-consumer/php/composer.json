{
    "name": "propsocket/rest-consumer-php",
    "description": "PropSocket REST API consumer sample (PHP) — lists an entity with offset pagination.",
    "type": "project",
    "license": "Apache-2.0",
    "require": {
        "php": ">=8.1",
        "guzzlehttp/guzzle": "^7.8"
    },
    "config": {
        "sort-packages": true
    }
}
