# PropSocket test API key (real reads; writes are dry-run). Get one from the dashboard.
PROPSOCKET_API_KEY=ps_test_...

# Optional: override the API base URL (defaults to the production v1 endpoint).
PROPSOCKET_BASE_URL=https://api.propsocket.io/v1
