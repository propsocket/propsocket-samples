# PropSocket webhook signing secret. Used to verify X-PropSocket-Signature
# (lowercase hex HMAC-SHA256 over the raw request body). Get it from the
# dashboard when you create the webhook endpoint.
PROPSOCKET_WEBHOOK_SECRET=...

# Optional: port for the built-in PHP server (defaults to 4000).
PORT=4000
