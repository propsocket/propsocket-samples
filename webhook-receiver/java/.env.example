# Signing secret PropSocket uses to HMAC-SHA256 the raw request body.
# Find it next to your webhook endpoint config in the dashboard.
PROPSOCKET_WEBHOOK_SECRET=...

# Optional: port to listen on (defaults to 4000).
PORT=4000
