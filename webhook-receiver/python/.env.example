# Signing secret used to verify the X-PropSocket-Signature header (HMAC-SHA256).
# Find it in the PropSocket dashboard under your webhook endpoint.
PROPSOCKET_WEBHOOK_SECRET=...

# Optional: port to listen on (defaults to 4000).
PORT=4000
