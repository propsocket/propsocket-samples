# Copy to .env and fill in. Never commit .env.
# The webhook signing secret from your PropSocket dashboard. Used to verify the
# X-PropSocket-Signature (lowercase hex HMAC-SHA256 over the raw request body).
PROPSOCKET_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxx

# Optional. Port to listen on (defaults to 4000).
PORT=4000
