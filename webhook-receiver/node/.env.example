# Signing secret used to verify the X-PropSocket-Signature HMAC.
# Find it in your PropSocket dashboard's webhook settings.
PROPSOCKET_WEBHOOK_SECRET=...

# Optional — port to listen on (defaults to 4000).
PORT=4000
