# Copy to .env and fill in. .env is gitignored — never commit a real secret.

# Signing secret used to verify the X-PropSocket-Signature HMAC.
PROPSOCKET_WEBHOOK_SECRET=...

# Optional: port to listen on (default 4000).
# PORT=4000
