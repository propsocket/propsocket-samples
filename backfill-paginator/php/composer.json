{
    "name": "propsocket/backfill-paginator-php",
    "description": "PropSocket backfill paginator sample (PHP) — pages every entity into a local upsert sink.",
    "type": "project",
    "license": "Apache-2.0",
    "require": {
        "php": ">=8.1",
        "guzzlehttp/guzzle": "^7.8"
    },
    "config": {
        "sort-packages": true
    }
}
