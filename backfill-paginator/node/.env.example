# PropSocket API key. Use a test key (reads are real; writes are dry-run).
PROPSOCKET_API_KEY=ps_test_...

# Optional — override the API base URL (defaults to the value below).
PROPSOCKET_BASE_URL=https://api.propsocket.io/v1
